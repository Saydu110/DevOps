# 🟩 Node.js Docker Image

This folder contains a `Dockerfile` to build and run a Node.js application inside a container.

## 🧱 Base Image
- `node:18-alpine` (lightweight and fast)

## 🧰 Build and Run
### 1️⃣ Build the image:
```bash
docker build -t node-app .
