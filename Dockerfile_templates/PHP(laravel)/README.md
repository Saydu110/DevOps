
---

## 🐘 `php/README.md`
```markdown
# 🐘 PHP (Laravel / General) Docker Image

This folder contains a `Dockerfile` for a PHP application (Laravel or simple PHP-FPM setup).

## 🧱 Base Image
- `php:8.2-fpm`

## 🧰 Build and Run
### 1️⃣ Build:
```bash
docker build -t php-app .
